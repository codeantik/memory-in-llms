# Ignore all .env files in root and subdirectories
.env
.env.*

# Ignore env files in any folder (explicit recursive pattern)
**/.env
**/.env.*

# Optional: allow example env files (good practice)
!.env.example